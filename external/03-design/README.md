# 03 — Design

- `diagrams/src/`: mã Mermaid có thể chỉnh sửa.
- `diagrams/rendered/`: SVG/PDF đã render.
- `ui-mvp-snapshots/`: ảnh chụp phiên bản UI cũ để đối chiếu; source UI hiện tại nằm ở root `app/`.
